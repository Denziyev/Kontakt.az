@using Kontakt.Core.Models;
@using Kontakt.Service.Responses;
@model MvcResponse<List<Discount>>
<div class="container">
    <table class="table table-sm">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Name</th>
                <th scope="col">StartDate</th>
                <th scope="col">EndDate</th>
                <th scope="col">CategoryLevel</th>
                <th scope="col">Update</th>
                <th scope="col">Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Data)
            {

                <tr>
                    <th scope="row">@item.Id</th>
                    <th>@item.Name</th>
                    <th>@item.StartDate</th>
                    <th>@item.EndDate</th>


                    <td><a asp-controller="discount" asp-action="MovetoDiscountCategory" asp-route-id="@item.Id"><i class="fa-solid fa-circle-info"></i> </a></td>
                    <td><a asp-controller="discount" asp-action="update" asp-route-id="@item.Id"><i class="fas fa-edit text text-primary"></i></a></td>
                    <td><a onclick="if (!confirm('Are you sure to delete?')){return false;}" asp-controller="Discount" asp-action="delete" asp-route-id="@item.Id" class="delete-item"><i class="fas fa-trash text text-danger"></i></a></td>
                </tr>
            }

        </tbody>
    </table>
    <div>
        <a asp-controller="discount" asp-action="create" style="display: block; text-decoration: none; color: white; width: 100%; height: 100%"> <div style="width:100%; " class="btn btn-outline-success">Create Discount </div></a>
    </div>

</div>